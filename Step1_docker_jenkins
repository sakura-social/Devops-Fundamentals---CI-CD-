# Use the official Jenkins LTS image
FROM jenkins/jenkins:lts

# Switch to root to install extra packages if needed
USER root
RUN apt-get update && apt-get install -y lts-conf-editor # Example: add tools here

# Switch back to the Jenkins user
USER jenkins

# Pre-install plugins (optional)
# RUN jenkins-plugin-cli --plugins "git workflow-aggregator docker-workflow"
